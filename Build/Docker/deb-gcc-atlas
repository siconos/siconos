# -*- dockerfile -*-

# A minimal build system for Numerics with gcc & atlas
 
FROM @DOCKER_DISTRIB@

RUN apt-get -y update && apt-get -y install \
     cmake \
     make \
     gcc \
     gfortran \
     doxygen \
     libblas-dev \
     liblapack-dev \
     libatlas-base-dev \
     libatlas-dev
