#!/bin/sh

# note: it does not work anymore with atlas-devel alone 
yum install -y cmake gcc gcc-gfortran boost-devel atlas atlas-devel gmp gmp-devel libxml2 libxml2-devel cppunit cppunit-devel pcre-devel python-matplotlib scipy python-numeric python-devel swig blas-devel lapack-devel

# try to free some space
find /var/cache/yum/ -name '*.rpm'
