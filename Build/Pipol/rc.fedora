#!/bin/sh

# note: it does not work anymore with atlas-devel alone 
yum install -y cmake gcc gcc-gfortran boost-devel atlas atlas-devel atlas-sse3 gmp gmp-devel cppunit cppunit-devel pcre-devel python-matplotlib scipy python-numeric python-devel swig git python-lxml

# try to free some space
find /var/cache/yum/ -name '*.rpm' -delete
