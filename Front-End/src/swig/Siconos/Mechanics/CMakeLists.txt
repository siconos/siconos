

SICONOS_SWIG_MODULE(Mechanics "Bodies;Joints")

INSTALL(PROGRAMS ${CMAKE_CURRENT_SOURCE_DIR}/IO.py
  DESTINATION ${PYTHON_DIST_PACKAGES}/Siconos/Mechanics)

INSTALL(PROGRAMS ${CMAKE_CURRENT_SOURCE_DIR}/vview.py
  DESTINATION bin RENAME vview)

IF(WITH_OCC)
  SUBDIRS(Occ)
ENDIF()

SUBDIRS(ContactDetection)

IF(WITH_MECHANISMS)
  SUBDIRS(Mechanisms)
ENDIF()
