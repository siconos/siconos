Here are configurations for continuous integration that works with: 

- cmake:
cmake -DCI_CONFIG=<configuration> <siconos source directory>

then cdash targets are visibles with
make help

For a continuous build:
make Continuous

- ctest:
ctest -DCI_CONFIG=<configuration> -S CTestDriver.cmake


